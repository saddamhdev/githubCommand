Perfect 👍 — using **`feature/saddamnvn`** is the right industry-style naming.

Follow this clean setup:

---

## 🔹 Step 1: Go to project

```bash
cd your-project-folder
```

---

## 🔹 Step 2: Update main first

```bash
git checkout main
git pull origin main
```

---

## 🔹 Step 3: Create new branch

```bash
git checkout -b feature/saddamnvn
```

---

## 🔹 Step 4: Push branch to GitHub

```bash
git push -u origin feature/saddamnvn
```

---

## 🔹 Verify

```bash
git branch
```

You should see:

```bash
* feature/saddamnvn
  main
```

---

## 🔹 Your daily workflow 🚀

```bash
# make changes
git add .
git commit -m "your task message"
git push
```

---

## 🔹 When main gets updated (VERY IMPORTANT)

Keep your branch synced:

```bash
git checkout main
git pull origin main
git checkout feature/saddamnvn
git merge main
```

OR better:

```bash
git pull --rebase origin main
```

---

## 🔹 Next step (after work done)

Create Pull Request on GitHub:

* base: `main`
* compare: `feature/saddamnvn`

---

## 🔥 Pro Tip for you (as a new joiner)

Later use naming like:

```
feature/user-login
feature/payment-api
fix/gateway-bug
```

---

If you want next:
👉 I can show full **PR + review + merge workflow (real company style)**
👉 Or Git mistakes to avoid (very important for beginners)
