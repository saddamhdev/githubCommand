Below is a **clean, copy-paste-ready documentation** you can keep in your project or personal notes.
It explains **how to change Git commit messages** clearly and professionally.

---

# 📘 Git Documentation: How to Change Commit Messages

This document explains **safe and correct ways** to change Git commit messages in different scenarios.

---

## 🔹 1. Change the **Last Commit Message** (Not Pushed)

### Use case

* You committed locally
* You haven’t pushed yet

### Command

```bash
git commit --amend -m "New commit message"
```

### Example

```bash
git commit --amend -m "Initial commit – Swosti Assignment"
```

---

## 🔹 2. Change the **Last Commit Message** (Already Pushed)

### Use case

* Commit is already on GitHub
* You want to rename it

### Steps

```bash
git commit --amend -m "Updated commit message"
git push --force
```

⚠️ **Warning**:
Use `--force` only when:

* You are working alone
* It’s an assignment or personal repo

---

## 🔹 3. Change an **Older Commit Message** (Interactive Rebase)

### Use case

* You want to change a commit that is **not the latest**

### Step 1: View commits

```bash
git log --oneline
```

Example:

```
06d6ba3 File removed
16b91d4 Create README.md
eababe6 Initial commit
```

---

### Step 2: Start interactive rebase

> Use `HEAD~N` where **N = number of commits to go back**

```bash
git rebase -i HEAD~2
```

---

### Step 3: Modify rebase list

Change:

```
pick 06d6ba3 File removed
```

To:

```
reword 06d6ba3 File removed
```

Save & exit editor.

---

### Step 4: Enter new commit message

Replace old message with:

```
Initial commit – Swosti Assignment
```

Save & exit.

---

### Step 5: Push changes

```bash
git push --force
```

---

## 🔹 4. Save & Exit Editor (Git Bash / Vim)

### Save and exit

```
Esc
:wq
Enter
```

### Exit without saving

```
Esc
:q!
Enter
```

---

## 🔹 5. Cancel Rebase (Emergency)

```bash
git rebase --abort
```

---

## 🔹 6. Change Editor to Nano (Optional)

If you don’t want vim:

```bash
git config --global core.editor "nano"
```

Save in nano:

* `Ctrl + O` → Enter
* `Ctrl + X`

---

## 🔹 7. Best Practice Commit Messages (Interview-Ready)

✅ Good:

* `Initial commit – Swosti Assignment`
* `Update README with setup instructions`
* `Refactor service layer and transaction logic`

❌ Bad:

* `update`
* `fix`
* `final`
* `test`

---

## 🔹 8. Safety Rules

| Situation         | Safe Method          |
| ----------------- | -------------------- |
| Local commit only | `git commit --amend` |
| Pushed commit     | Amend + `--force`    |
| Team project      | ❌ Avoid force push   |

---

## ✅ Summary

* Use `--amend` for last commit
* Use `git rebase -i` for older commits
* Always `git push --force` if commit is already pushed
* Keep commit messages clean and professional

---

If you want, I can:

* Convert this into **PDF / README.md**
* Simplify for **beginners**
* Add **real interview examples**

Just tell me 👍
